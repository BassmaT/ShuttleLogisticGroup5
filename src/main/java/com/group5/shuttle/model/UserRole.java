package com.group5.shuttle.model;

/**
 * Defines the role/persona of a user in the system.
 * Used for access control and AI chat behavior selection.
 */
public enum UserRole {
    PLANNER,
    SECURITY,
    TECHNICIAN,
    LOGISTICIAN
}